<footer class="content-info" role="contentinfo">
    <div class="container">
        <div id="footer-content">
            <div class="row">
                <div class="col-sm-6">
                    <div class="row">
                        <div class="col-sm-12">
                            <img src="{{ header_image }}" />
                        </div>
                    </div>
                    <div class="row">
                        <div class="col-sm-6">
                            <address>
                                <strong>{{ address['address-1-country'] }}</strong><br>
                                {{ address['address-1-address-line-1'] }}<br>
                                {{ address['address-1-address-line-2'] }}<br>
                                <abbr title="Phone">P:</abbr> {{ address['address-1-telephone'] }}
                            </address>
                        </div>
                        <div class="col-sm-6">
                            <address>
                                <strong>{{ address['address-2-country'] }}</strong><br>
                                {{ address['address-2-address-line-1'] }}<br>
                                {{ address['address-2-address-line-2'] }}<br>
                                <abbr title="Phone">P:</abbr> {{ address['address-2-telephone'] }}
                            </address>
                        </div>
                    </div>
                </div>
                <div class="col-sm-6">
                    <div class="row">
                        <div class="col-sm-6">
                            <h4>Our Brands</h4>
                            <ul class="list-unstyled">
                                {% for brand in brands %}
                                <li>{{ brand.title }}</li>
                                {% endfor %}
                            </ul>
                        </div>
                        <div class="col-sm-6">
                            <div class="row">
                                <h4>Education</h4>
                            </div>
                            <div class="row">
                                <h4>Help and sport</h4>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</footer>
